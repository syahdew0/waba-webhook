{
  "name": "wa-webhook",
  "version": "1.0.0",
  "main": "src/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/app.js",
    "db:migrate": "knex migrate:latest",
    "db:rollback": "knex migrate:rollback",
    "crm:dev": "npm --prefix crm-ui run dev",
    "crm:build": "npm --prefix crm-ui run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "axios": "^1.13.5",
    "body-parser": "^2.2.2",
    "express": "^5.2.1",
    "knex": "^3.1.0",
    "mysql2": "^3.17.3"
  },
  "devDependencies": {
    "knex-cli": "^1.0.1"
  }
}
